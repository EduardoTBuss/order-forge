# Shell scripts and the backend entrypoint run inside Linux containers, so they
# must keep LF line endings regardless of the contributor's platform / autocrlf.
*.sh text eol=lf
entrypoint.sh text eol=lf
